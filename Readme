git clone https://github.com/wzd0328/rl_sky_dqn.git

pip3 install torch torchvision //先根据cuda版本安装pytorch(这一步我之前没注意不同环境是隔离的就没下，先执行的后面几步)

pip install opencv-python //安装OpenCV库

apt-get update && apt-get install -y libgl1 libglib2.0-0 libsm6 libxext6 libxrender-dev libx11-6 libfontconfig1 libice6 libglib2.0-0 //在系统层面安装 libGL 和其他 OpenCV 常用的依赖

pip install gymnasium //安装gymnasium 模块

pip install matplotlib //安装matplotlib 模块